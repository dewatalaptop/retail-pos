JWT_SECRET=replace-with-a-long-random-string

# Platform-level Google AdSense (this app's own monetization — applies to
# every store's ad slots identically, not configurable per store owner).
# Leave blank to show the honest "ad space" placeholder instead of ads.
ADSENSE_CLIENT_ID=
ADSENSE_SLOT_FOOTER=
ADSENSE_SLOT_REPORTS=
